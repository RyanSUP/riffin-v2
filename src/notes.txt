<HeaderLinks>
  X userNavLinks []
  X createButton 
  X loginButton

  If(desktop) {
    {user
      <Create button>
      <Avatar props.userLinks>
    }
    {!user
      <Create button> // clicking navs to login
      <LoginButton>
    }
  } else (on mobile) {
    {user
      squishedlinks = userNavLinks + createButton
      <EllipseMenu links = {squishedlinks}>
    }
    {!user
      squishedlinks = login + createButton
      <EllipseMenu links = {squishedlinks}>
    }
  }


</>